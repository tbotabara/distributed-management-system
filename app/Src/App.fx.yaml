App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =ClearCollect(colHorizontalMenu,
        
        {Title:"Dashboard",Screen:landing_screen, Id:1, ParentTab: ""},
        
        {Title:"Draft",Screen:claim_draft_screen, Id:2, ParentTab: ""},
        
        {Title:"KPI",Screen:menu_screen, Id:3, ParentTab: ""}
        
        );
        
        Set(varMenuPrimaryColor, RGBA(255, 255, 255, 1));
        Set(varMenuSecondaryColor, RGBA(164, 38, 44, 100));
        
        Set(varTabSelected, 0);
    StartScreen: =login_screen

